version: '3'

services:
  jupyter:
    image: jupyter/base-notebook:latest
    container_name: jupyter_notebook
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/home/dzikri/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - JUPYTER_TOKEN= 
